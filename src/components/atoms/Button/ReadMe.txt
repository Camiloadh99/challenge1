Este componente cuenta con:

    1. Variantes de color (variant -> string):

        - primary
        - secondary
        - success
        - danger
        - warning
        - info
        - light
        - dark

    Las cuales se pueden usar al ser escritas o con el enum "colorVariants" en interfaces

    2. tipo de boton (outlined ->boolean):
     si se agrega el prop outlined el estilo del boton cambia

    3. tamaño del boton (size -> string)

        - small
        - medium
        - large

    4. Otras props:

        - onClick
        - disabled
        - type = "button"
        - className = ""
        - style = {}
        - label = "Button"
        - name
        - id